# Server Configuration
server.port=8080

# Database Configuration - PostgreSQL (Neon.tech)
# Si DATABASE_URL está definida, se usará automáticamente (ver DatabaseConfig.java)
# Si no, usa estas variables individuales:
SPRING_DATASOURCE_URL=jdbc:postgresql://ep-old-union-ahnvrr9v.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
SPRING_DATASOURCE_USERNAME=neondb_owner
SPRING_DATASOURCE_PASSWORD=npg_7k0XjmlVvZaP
spring.datasource.url=${SPRING_DATASOURCE_URL:}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging
logging.level.com.inventory=INFO

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false
